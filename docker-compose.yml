version: "2"
services:
  node:
    build: ./
    image: node-docker:v1
    volumes:
      - ./app/:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    ports:
      - 3000:3000
      - 9229:9229
volumes:
  node_modules: {}

